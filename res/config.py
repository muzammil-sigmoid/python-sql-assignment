from res.secret import db_password
config = {
    "db_password" : db_password, # replace it with your own password
    "db_user" : "postgres",   # replace it with your own username
    "db_name": "assignment", # replace it with your own db_name
    "out_folder_path": "./out_data/"   # replace it with your own data folder

}
